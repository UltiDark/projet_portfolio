{% extends 'base.html.twig' %}

{% block title %}{{intro.titre}}{% endblock %}

{% block body %}
    <h1>Intro</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ intro.id }}</td>
            </tr>
            <tr>
                <th>Paragraphe</th>
                <td>{{ intro.paragraphe }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('intro_index') }}">back to list</a>

    <a href="{{ path('intro_edit', {'id': intro.id}) }}">edit</a>

    {{ include('intro/_delete_form.html.twig') }}
{% endblock %}
